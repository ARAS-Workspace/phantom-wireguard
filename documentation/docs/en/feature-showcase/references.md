# Recording References

Some of the terminal recordings in this section are generated by automated workflows.

## Asset Structure

```
documentation/docs/assets/static/recordings/feature-showcase/
├── api/
├── cli/
└── manuel/
```

## Automated Recording Systems

### API Recordings

**Directory:** `api/`

**Workflow:** [generate-api-recordings.yml](https://github.com/ARAS-Workspace/phantom-wg/blob/main/.github/workflows/generate-api-recordings.yml)

Automated recording of scripts using `phantom-api` commands. Manually triggered via `workflow_dispatch` on GitHub Actions.

**Recording Tool:** `documentation/tools/scripts/recording_automations_api/`

| File          | Description                                   |
|---------------|-----------------------------------------------|
| `automate.sh` | Runs all scripts sequentially                 |
| `common.sh`   | Common functions (ssh_connect, run_command)   |
| `api/*.sh`    | Separate script for each API operation        |

**Available Recordings:**

- `server_status` - Server status query
- `add_client` - Client addition
- `list_clients` - Client list
- `export_client` - Configuration export
- `latest_clients` - Recently added clients
- `tweak_settings` - Setting modifications
- `change_subnet` - Subnet change
- `get_firewall_status` - Firewall status
- `service_logs` - Service logs
- `restart_service` - Service restart
- `remove_client` - Client removal
- `dns_compact` - DNS management

---

### CLI Recordings

**Directory:** `cli/`

**Workflow:** [generate-cli-recordings.yml](https://github.com/ARAS-Workspace/phantom-wg/blob/main/.github/workflows/generate-cli-recordings.yml)

Automated recording of the interactive `phantom-wg` menu. Works with YAML-based workflow definitions.

**Recording Tool:** `documentation/tools/scripts/phantom-recorder/`

| File                  | Description                           |
|-----------------------|---------------------------------------|
| `phantom_recorder.py` | Main recording engine (pexpect-based) |
| `workflows/*.yaml`    | Scenario definition files             |

**Available Recordings:**

- `interactive_cli` - Main menu navigation

---

### Manual Recordings

**Directory:** `manuel/`

Scenarios recorded manually in the test environment. Used for recordings that require multiple servers
and complex configurations.

**Scenario Documents:** `documentation/tools/scripts/recording_environment/scenarios/`

- `multihop_compact.md` - Multihop VPN setup scenario
- `ghost_compact.md` - Ghost Mode setup scenario

